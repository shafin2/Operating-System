#include <bits/stdc++.h>

using namespace std;

int main(){
	int age;
	cout << "Enter name " << endl;
	cin >> name;
	string str = "echo "+to_string(age)+" | cat >> abc.txt";
	system(str.c_str());
	return 0;
}
